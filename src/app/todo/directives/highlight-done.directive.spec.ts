import { HighlightDoneDirective } from './highlight-done.directive';

describe('HighlightDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDoneDirective();
    expect(directive).toBeTruthy();
  });
});
